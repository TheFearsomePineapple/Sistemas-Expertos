public class AlarmaDeTemperatura {

    public static void main(String[] args) {
        int temperaturaActual = 32; // Supongamos que la temperatura actual es de 32 grados Celsius.

        if (temperaturaActual >= 30) {
            activarAlarma();
        }
    }

    public static void activarAlarma() {
        System.out.println("La alarma de temperatura se ha activado debido a una temperatura alta.");
        // Aquí puedes agregar código para activar la alarma real, como enviar notificaciones, sonar una sirena, etc.
    }
}
